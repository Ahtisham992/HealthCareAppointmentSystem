@page
@model HealthCareAppointmentSystem.Areas.Identity.Pages.Account.ResendEmailConfirmationModel
@{
    ViewData["Title"] = "Resend email confirmation";
}

<div class="row justify-content-center">
    <div class="col-md-6" style="margin: 0 auto;">
        <form method="post" class="form-card text-center" style="max-width: 450px;">
            <div style="font-size: 3rem; margin-bottom: 1rem;">✉️</div>
            <h2 style="color: var(--primary); font-weight: 800; letter-spacing: -0.5px;">@ViewData["Title"]</h2>
            <p style="color: var(--text-muted); margin-bottom: 2rem;">Enter your email to receive a new confirmation link.</p>
            
            <div asp-validation-summary="All" class="text-danger validation-summary" role="alert"></div>
            
            <div class="form-group" style="text-align: left;">
                <label asp-for="Input.Email" class="form-label"></label>
                <input asp-for="Input.Email" class="form-control" aria-required="true" placeholder="name@example.com" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            
            <div>
                <button type="submit" class="btn btn-primary w-100" style="padding: 0.9rem; font-size: 1.1rem;">Resend</button>
            </div>
            
            <div style="margin-top: 1.5rem; font-size: 0.9rem;">
                <a asp-page="./Login" style="color: var(--text-muted); text-decoration: underline;">Back to Login</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
